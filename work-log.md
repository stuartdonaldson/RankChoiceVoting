## 2026-07-05 19:15:24

### Summary:
Migrated RankChoiceVoting from OneDrive/Proj to /mnt/c/dev and adopted F3Go30's clasp build/deploy methodology. Moved all 7 GAS files into a `script/` rootDir; retired the loose `.clasp.json-dev`/`.clasp.json-prod` (scriptIds relocated to gitignored `local.settings.json`, keyed sitScriptId/prodScriptId). Added `tools/manage-deployments.js` (SIT/PROD targets, owns `.clasp.json`, stamps `script/version.js`, updates one named deployment per project), `package.json` (deploy:sit/deploy:prod/push/release:*), `local.settings.json[.example]`, `.envrc`, and updated `.gitignore`. Stood up a webapp: copied GActionSheet `webSurvey.js` + `webSurveyPage.html` verbatim, added `WebApp.js` doGet router (?cmd=survey), a minimal `GasLogger.js` shim, `version.js`, and a webapp block in `appsscript.json`. Offline verification passed: node --check on all 11 JS files, module load/exports, JSON validity, version-stamp/counter logic (SIT->0.0.1.1, PROD->0.0.2 + build reset), npm install (32 pkgs, 0 vuln). Not committed.

### Key Learnings:
- clasp only reads its credential file from the lowercase `clasp_config_auth` env var (exact match); `CLASP_CONFIG` is a no-op that silently falls back to `~/.clasprc.json`. Never use bare `clasp`.
- `manage-deployments.js` requires exactly one active non-@HEAD named deployment per script project; the initial Web app deployment must be created once in the Apps Script editor before automated deploys can update it.
- `.envrc` is ignored via the user's global `~/.gitignore_global`, so direnv config stays machine-local by design (consistent with F3Go30).
- clasp `rootDir` should be relative (`.` or `script`) rather than an absolute host path so config is location-independent after a move.

### Deferred / Manual:
- User must: `clasp_config_auth=~/.clasprc-rankchoicevoting.json clasp login`; `npm run deploy:sit`; create a Web app deployment in the editor for SIT and PROD.
- webSurveyPage.html still carries GActionSheet branding ("Northlake Topic and Ideas Survey").
- Wiring Survey-sheet submissions into processRankedChoiceVotes/processCondorcetVoting.

## 2026-07-05 22:22:43

### Summary:
Refactored RankChoiceVoting from a single hardcoded "Survey" sheet into a multi-survey architecture. Added `script/SurveyModel.js` defining the `Survey-<name>` sheet layout: config rows (Title/Description/Footer/Contact/Accept-New) → `Results` marker → analysis output → `Responses` marker → header (Date|Name|Weight|Comment|candidates...) → response rows. Rewrote `webSurvey.js` + `webSurveyPage.html` for `?cmd=survey&id=<name>`, pulling branding from the sheet's own config via `google.script.url.getLocation()`, with a generic "no survey" fallback when id is missing/unknown. Added `webAdmin.js` for `?cmd=admin`: lists all `Survey-*` sheets, runs RCV+Condorcet analysis in-browser (writing results back into each survey's Results section), and adds a Create New Survey form plus a per-survey Edit form (Title/Description/Footer/Contact/Accept-New) using plain GET forms with no JS. Refactored `processRCV.js`/`processCondorcet.js` to accept candidate/ballot data directly — extracted `runRankedChoiceVoting` out of `processRankedChoiceVotes`, made `processCondorcetVoting` take optional candidates/ballots, and added a per-ballot `weight` field — while the old fixed-sheet menu flow (Candidates/Candidate Responses) still works unchanged. `WebApp.js` now routes `cmd=admin` and added a `doPost` action `setWebappUrl`; `onOpen.js` gained an "Open Survey Admin Page" menu item and a factored-out `formatCandidateSummaryHtml`. `tools/manage-deployments.js` now calls a new `tools/callWebapp.js` (adapted from F3Go30) to stamp `WEBAPP_URL` after every PROD deploy. This is a distinct, later pass than the 2026-07-05 19:15:24 entry above, which only had the single-sheet `webSurvey.js`.

### Key Learnings:
- GAS web app client JS can read query params passed to the doGet URL via `google.script.url.getLocation()` — the supported way to get `?id=` into page JS without templating.
- Survey Results/Responses sections can be resized independently by inserting/deleting rows strictly between the two section markers; everything below (including Responses) shifts as one block, so response data is never disturbed by a differently-sized analysis write.

### Deferred / Manual:
- `script/appsscript.json` still deploys with `"access": "ANYONE_ANONYMOUS"` — the admin page and all survey data are publicly viewable by anyone with the URL. Needs an explicit decision (tighten to `ANYONE` w/ login, or add an admin-secret gate like F3Go30).
- New `Survey-<name>` sheets are created with `[TODO: ...]` placeholder config text (Title/Description/Footer/Contact) that must be filled in per survey.
- No UI to seed a survey's initial candidate list — candidates must be typed directly into the Responses header row, or seeded by respondents if Accept-New is on.
- No migration path was built from the old single "Survey" sheet or the `Candidates`/`Candidate Responses` sheets into the new `Survey-<name>` format.

## 2026-07-05 22:55:00

### Summary:
Completed the GAS-webapp upgrade for RankChoiceVoting. Prior groundwork (this session, before my tasks): rewrote GasLogger.js (Axiom sink + Drive fallback, PII masking, module.exports), WebApp.js (doGet/doPost router with a minimal cmd=admin JSON API gated by ADMIN_SHARED_SECRET), onOpen.js (About dialog), version.js (APP_AUTHOR/APP_CONTACT), and added module.exports to SurveyModel.js/webAdmin.js for Node testability.

My work (tasks 7-12):
- Rewrote tools/callWebapp.js into a general `?cmd=admin` API client: injects sitAdminSecret/prodAdminSecret automatically except for the ungated bootstrapSecret/setWebappUrl actions, supports --body/--env, and adds a bootstrapSecret convenience (generates a 32-char hex secret, POSTs it, saves it locally on success). Kept backward compatibility with manage-deployments.js's `setWebappUrl --env prod` call.
- Added tools/query-axiom.js, a Node port of F3Go30's query_axiom.py (APL query builder, --limit/--since/--name/--where/--raw, never prints the token).
- Populated local.settings.json with the real Axiom dataset ("survey") and ingest token, plus placeholder sitAdminSecret/prodAdminSecret/GAS_LOGGER_PARENT_FOLDER_ID/prodDeploymentId; mirrored placeholders (angle-bracket descriptions) into local.settings.json.example.
- Built test/fakeGas.js, a reusable in-memory GAS shim (FakeSpreadsheet/FakeSheet/FakeRange with sparse row_col cell storage, plus PropertiesService/Logger/Utilities/LockService stubs), and used it to write test/test_survey_model.js (create/list/find/config round-trip/candidates+response round-trip/duplicate+invalid id) and test/test_gas_logger.js (buildAxiomRows_/maskPiiForLog_/maskRecipientListForLog_). Ported test/test_manage_deployments.js and test/test_callwebapp.js from F3Go30. Wired all four into `npm test` — all passing.
- Created tools/smokeTest.js (integration test against a live SIT deployment: bootstrap secret, createSurvey, listSheets, getSheet, setSheet, getSheet round-trip, deleteSheet cleanup).
- Ran `npm run deploy:sit` successfully (clasp auth via ~/.clasprc-f3go30.json was already in place), then ran tools/smokeTest.js against the live deployment — ALL STEPS PASSED, including createSurvey via the JSON API. Additionally hand-verified the browser-facing `?cmd=admin&action=create&id=...` GET path directly with curl-equivalent Node requests — it also succeeded, returning the expected "Survey created" edit form. Cleaned up all test sheets afterward (verified via listSheets).
- Conclusion on the previously-reported "adding a survey fails to create a sheet" bug: it does NOT reproduce against the current (already-rewritten) SurveyModel.js/WebApp.js/webAdmin.js code, via either the admin JSON API or the browser GET create form, on a live SIT deployment. The bug was most likely present in an earlier version of the code (before this session's SurveyModel/WebApp rewrite) or was environment/transient. No code change was made for task 5 beyond what was already in place; this is a verified-fixed / could-not-reproduce conclusion backed by live evidence, not a blind fix.
- Added explicit oauthScopes to script/appsscript.json (spreadsheets, script.external_request, drive, script.container.ui, script.scriptapp) to cover UrlFetchApp (Axiom + Drive PATCH), DriveApp, SpreadsheetApp, and HtmlService/menu UI usage explicitly, since F3Go30's own appsscript.json relies on implicit scope autodetection and doesn't list any — this project now declares them explicitly instead.

### Key Learnings:
- The Axiom credential provided (`axiomToken`/`axiomQueryToken`, same value) is ingest-only — querying it via the Axiom `_apl` query API returns a real HTTP 403 ("token does not have access to resource: query with action: read"). This confirms tools/query-axiom.js's request shape and auth wiring are correct; a separate query-scoped token is needed to actually pull data with it. User later supplied a real query-scoped token; updated `axiomQueryToken`. Dataset name corrected to `surveys` (plural) per user, and the `AXIOM_TOKEN`/`AXIOM_DATASET` script properties were pushed to SIT via `node tools/callWebapp.js setScriptProperties`.
- Live smoke-testing (deploy + exercise the real deployed web app) is much stronger evidence than a fake-GAS unit harness alone for confirming a live-environment bug is fixed — the fake-GAS harness proved the pure model logic was fine, but only the live SIT run proved the whole deployed stack (WebApp.js routing, ADMIN_SHARED_SECRET gate, PropertiesService, SpreadsheetApp.insertSheet under a real webapp identity) works end to end.
- **Axiom ingest silently failed after adding explicit `oauthScopes` to appsscript.json**: switching from Apps Script's implicit/auto-detected scope grants to an explicit `oauthScopes` manifest list requires a fresh authorization pass — the prior implicit grant doesn't carry over. Since the web app runs `executeAs: USER_DEPLOYING` + `access: ANYONE_ANONYMOUS`, an anonymous request executes as the deploying user's stored authorization snapshot; if that snapshot doesn't cover a scope the code exercises (here, `UrlFetchApp.fetch` → `script.external_request`), the call throws `permission denied` with no possible interactive prompt (no user session exists in an anonymous request). Diagnosed via a new gated `getDiagnostics` admin action with an opt-in `testAxiomIngest` flag that fires a real Axiom ingest and returns the raw HTTP result/thrown exception (kept permanently — never exposes the token itself, only presence/length and the test-call outcome). Fixed by adding `authorizeExternalRequestScope()` to `script/GasLogger.js` — a function whose sole purpose is to exercise `UrlFetchApp.fetch` so running it once from the Script Editor (logged in as the deploying account) surfaces the missing scope's consent dialog. This is a one-time grant per Apps Script project (SIT and PROD are separate projects with independent authorization state) — PROD will need the same manual step once deployed there. `getScriptProperty` does NOT trigger this consent (it only touches PropertiesService, a different scope) — a function must actually call the API tied to the missing scope.
- End-to-end Axiom pipeline confirmed live on SIT after the fix: a real `?cmd=admin` webapp hit produced `doGet`/`webapp.admin` events queryable via `tools/query-axiom.js` within ~8 seconds of ingest.

### Deferred / Manual:
- PROD deploy has not happened yet; when it does, `authorizeExternalRequestScope()` must be run once from the PROD script editor (same one-time OAuth consent as SIT) before Axiom ingest will work there.

## 2026-07-06 06:20 (approx, same session continued)

### Summary:
User supplied a browser HAR (`/mnt/c/tmp/script.har`) captured while reproducing "blank page after creating a survey." Analysis of the trace showed Apps Script serving an "Authorization required" dialog (`This app might not work as expected without providing all requested permissions`) on page load, followed by the create-survey form submission (`userCodeAppPanel?cmd=admin&action=create&id=ABC-2026-7`) returning a bare 866-byte iframe shell with no embedded `goog.script.init(...)`/`userHtml` payload at all — i.e. the browser-side sandbox refused to deliver real page content, while server-to-server `tools/callWebapp.js` POST calls (bypassing that sandbox layer) kept working. Root cause: `script/appsscript.json`'s explicit `oauthScopes` still declared `https://www.googleapis.com/auth/drive`, which is unused by any current code path (`GAS_LOGGER_PARENT_FOLDER_ID` is unset, so GasLogger's Drive-fallback branch never runs — Axiom is the active sink) and had never been granted (the earlier `authorizeExternalRequestScope()` run only exercises `UrlFetchApp`/`script.external_request`). Apps Script appears to gate anonymous-viewer execution behind full authorization of every declared scope, not just the ones a given request actually uses. Fix: removed the unused `drive` scope from `appsscript.json` (best-practice minimal-scope principle — don't declare capabilities the app doesn't exercise), redeployed to SIT (@7). Awaiting user confirmation from a hard-refreshed browser session that the banner is gone and create-survey now renders.

### Key Learnings:
- A GAS web app's browser-rendered UI (form submissions/link navigation inside the HtmlService sandbox iframe) and direct server-to-server POST/GET calls to the same `/exec` endpoint are NOT equally gated by pending-authorization state — the sandboxed browser UI path can silently return an empty shell (no error surfaced) while raw HTTP calls to the identical backend succeed. This means "the API works" is not sufficient evidence that "the UI works" when OAuth scope authorization is incomplete; both must be checked independently.
- Prefer the narrowest oauthScopes that current code actually exercises rather than a broad, speculative set (e.g. don't declare `drive` for a Drive-logging fallback path that's dormant while `GAS_LOGGER_PARENT_FOLDER_ID` is unset) — an unused declared scope can silently block anonymous-viewer execution project-wide until granted, with no clear error message pointing at the cause.

### Deferred / Manual:
- If removing the `drive` scope does not clear the "Authorization required" banner, the remaining scope most likely to be ungranted is `script.scriptapp` (never explicitly exercised standalone) — would need its own one-off "authorize" function run from the editor, same pattern as `authorizeExternalRequestScope()`.
- If Drive-fallback logging is ever wanted later (setting `GAS_LOGGER_PARENT_FOLDER_ID`), the `drive` (or narrower `drive.file`) scope must be re-added to `appsscript.json` and re-authorized via the same editor-run pattern before it will work.

## 2026-07-06 06:30 (approx, same session continued)

### Summary:
User supplied an updated HAR captured after the `drive`-scope removal — the "Authorization required" banner and the blank 866-byte create-survey response were both still present, disproving the scope-count theory as the (sole) cause. Re-examining the trace against Apps Script's iframe-sandboxing model surfaced the real root cause: every `<form>`/`<a href>` in `webAdmin.js` that navigates to a new doGet-rendered page (create-survey form, Edit/Run Analysis links, Save form, "Back to survey list" links) lacked `target="_top"`. Without it, submitting a form inside HtmlService's sandboxed iframe is handled by the sandbox's internal `userCodeAppPanel` navigation shim rather than a real top-level request to `.../exec` — which is why direct `curl`/`callWebapp.js` calls to the same backend always worked (they bypass the sandbox and its `userCodeAppPanel` layer entirely) while real in-browser clicks produced an empty shell. Added `target="_top"` to every internal admin-page form/link (create form, edit form, Edit/Run Analysis buttons, both "Back to survey list" links); left the survey-link and admin-page links that already used `target="_blank"` alone (those open a real new tab, which also escapes the sandbox correctly). Redeployed to SIT.
- Also added a "vX.X.X (TARGET) — deployed <date>" footer to every admin-page render (`webAdmin.js`'s `_versionFooterHtml_()`, appended in `_handleAdmin`) and to the survey respondent page (`webSurveyPage.html`'s new `#appVersionFooter`, populated from `appVersion`/`appDeployTarget` fields added to `SurveyModel.js`'s `getSurveyForRespondent_` result) — user asked to be able to visually confirm which deployed build is live from the page itself, not just via the `getDiagnostics` admin API.
- Separately, user reported the "Open Survey Admin Page" spreadsheet menu item pointed at the wrong deployment. Cause: `tools/manage-deployments.js` only ran `callWebapp.js setWebappUrl` after a **PROD** deploy (mirroring F3Go30, whose menu items are only meant for the Template project) — this project's SIT project's `WEBAPP_URL` script property had never been stamped, so `onOpen.js`'s `_getWebAppUrl_()` fell back to `ScriptApp.getService().getUrl()`, which resolves incorrectly (empty/editor URL) when called from a menu/editor context rather than an actual running web request. Fixed by stamping `WEBAPP_URL` after **every** deploy (SIT and PROD each have their own script property to stamp, since they're separate Apps Script projects) rather than PROD-only. Ran `node tools/callWebapp.js setWebappUrl --env sit` immediately to fix the currently-live SIT deployment without waiting for the next `npm run deploy:sit`.

### Key Learnings:
- **Apps Script's HtmlService sandbox iframe requires `target="_top"` on any `<form>`/`<a href>` meant to load a genuinely new doGet-rendered page.** Without it, the browser's in-sandbox navigation handling (`userCodeAppPanel` on the `*-script.googleusercontent.com` sandbox host) intercepts the request and does not properly re-render — producing a valid-looking 200 response that is actually just an empty iframe shell, with no error surfaced anywhere. `target="_blank"` (opening a real new tab) also correctly escapes the sandbox. This is easy to miss because curl/API testing of the identical backend action always succeeds (it bypasses the sandbox layer entirely) — a passing integration test against the JSON admin API is NOT sufficient evidence that the human-facing HTML UI works; the two paths can silently diverge.
- This explains (and retroactively supersedes) the earlier session's "Authorization required"/scope-count investigation — that banner and the blank page were parallel-but-separate symptoms being conflated; the OAuth scope work was still worth doing (narrower scopes are better practice) but was not the actual fix for the reported bug.
- Each Apps Script deployment environment (SIT/PROD here) is a fully separate script project with its own independent `WEBAPP_URL` (and other) script properties — a fix/stamp applied to one environment does not carry over to the other; automation that should apply to "every deploy" needs to explicitly loop over environments rather than special-casing just one (the original PROD-only stamping logic was copied from F3Go30, where the asymmetry is intentional because F3Go30's menu items are only meant to be used from the Template/PROD project — that asymmetry did not hold for this project, where the menu can legitimately be opened from either environment during testing).

### Deferred / Manual:
- User should hard-refresh the admin page and confirm: (1) survey creation now renders the edit page instead of going blank, (2) the version footer is visible and matches the latest deployed build, (3) the "Open Survey Admin Page" menu now opens the correct SIT URL.
- PROD deploy still pending — when it happens, `npm run deploy:prod` will now automatically stamp PROD's own `WEBAPP_URL` (previously it already did; no change in behavior there), and `authorizeExternalRequestScope()` still needs its one-time manual run in the PROD script editor (per the earlier deferred item).

## 2026-07-06 06:40 (approx, same session continued)

### Summary:
User reported the blank-page-on-create bug was STILL present after the `target="_top"` fix + SIT redeploy (@8). Re-examined the fix without a fresh HAR (none was supplied yet) and found it was incomplete: `target="_top"` alone escapes the HtmlService sandbox's nested iframe, but every affected form/link still used a RELATIVE URL (`href="?cmd=admin..."`, forms with no `action` attribute) — a relative URL resolves against the *current document's own base*, which for sandboxed content is the `script.googleusercontent.com` sandbox origin, not the real `script.google.com/macros/.../exec` URL. So `target="_top"` was navigating the whole tab to a bare sandbox-internal URL never meant to be viewed standalone, which would still render blank (or worse than before). Fixed by adding an explicit ABSOLUTE `action="<webAppUrl>"` (forms) / `href="<webAppUrl>?cmd=...` (links) built from `_getWebAppUrl_()` (already used elsewhere in this file for the survey link) alongside `target="_top"`, in `_renderAdminList_`, `_renderCreateForm_`, `_renderAdminEdit_`, and `_renderAdminAnalysis_`. Redeployed to SIT (@9); `setWebappUrl` also re-ran automatically as part of the deploy (per the prior fix), keeping `WEBAPP_URL` current. Re-verified the backend create/getSheet/deleteSheet round-trip via `callWebapp.js` once more (still works, as expected — this was never the layer with the bug). Awaiting a fresh browser test / HAR from the user to confirm the actual UI now renders correctly, since the backend-only verification cannot exercise the sandbox/iframe navigation path that was actually broken.

### Key Learnings:
- `target="_top"` and an absolute URL are BOTH required together for Apps Script HtmlService sandbox escape — either alone is insufficient. `target="_top"` without an absolute URL still resolves the relative reference against the wrong (sandbox) origin.
- This project's admin pages already had a working precedent for the correct pattern (the survey link in `_renderAdminEdit_`, built as `_getWebAppUrl_() + '?cmd=survey&id=...'` with `target="_blank"`) — the bug was introduced by not applying that SAME absolute-URL construction to the newly-added `target="_top"` fix on the other forms/links, only fixing the target attribute and not the URL itself.

### Deferred / Manual:
- Still need user confirmation (ideally with a fresh HAR) that create-survey now actually renders the edit page in the browser, not just via the backend admin API.

### Resolution:
User confirmed the create-survey flow now works in the browser after the `target="_top"` + absolute-URL fix (@9). Blank-page bug closed.

## 2026-07-06 (later same day)

### Summary:
User asked for three UI improvements to the admin page: (1) mobile-responsive scaling, (2) a direct "view the survey itself" link per list entry (previously only Edit/Run Analysis existed), (3) a new per-survey "additional information" field, discoverable without relying on tooltips (which don't have a reliable trigger on touch devices).
- **Mobile**: added `.addMetaTag('viewport', ...)` to the admin page's `HtmlOutput` (was missing — GAS only reliably honors a mobile viewport via `addMetaTag`, not a plain `<meta>` tag in the served HTML, same reason `webSurvey.js` already did this). Replaced the survey list's `<table>` (forces horizontal scroll on narrow screens) with a flexbox "card" layout (`.survey-card`) that stacks naturally; action buttons use `flex-wrap` so they reflow instead of overflowing. Added `min-height:44px` touch targets throughout (WCAG 2.5.5) to both the admin page and `webSurveyPage.html` (which already had a correct viewport tag and mostly-responsive layout, but buttons lacked touch-sized targets and the Submit/Back buttons could crowd on narrow screens — made buttons full-width by default, widened the drag `.handle` touch target).
- **View link**: each survey card now includes a "View Survey" button (absolute URL + `target="_blank"`, same safe pattern as the existing edit-page survey link) alongside Edit/Run Analysis.
- **Additional info**: added a 6th survey config field, `Info` (admin-only free-text notes), to `SurveyModel.js`'s sheet layout — Title/Description/Footer/Contact/Accept-New/Info now occupy rows 1-6 (was 1-5), shifting the Results marker to row 8 and Responses marker to row 10 (was 7/9). `_SURVEY_CONFIG_ROWS` updated to match (used as the row-count fallback when the Results marker is absent). `getSurveyForRespondent_` builds its RPC response field-by-field rather than spreading the config object, so `Info` is never exposed to survey respondents — admin-only by construction, not just by convention. Rendered in the admin list as a native `<details><summary>ℹ️ More info</summary>...</details>` disclosure — chosen specifically over a `title=""` tooltip because `<details>` opens identically on tap or click, needs no JS, and is screen-reader accessible; omitted entirely from a card when Info is empty, so the list stays uncluttered for surveys with no notes. Added the `Info` textarea to the edit form and wired it into `_handleAdminSave_`.
- Updated `test/test_survey_model.js`'s hardcoded marker-row assertions (7/9/10 → 8/10/11) to match the new layout. Deployed to SIT (@10) and verified the new sheet layout server-side via `callWebapp.js createSurvey`/`getSheet`/`deleteSheet` (Info row present at the right position, 11 total rows for a fresh skeleton vs. the old 10).

### Key Learnings:
- `SurveyModel.js`'s config read/write functions (`readSurveyConfig_`/`writeSurveyConfig_`) are already marker-relative (scan column A for the `Results` marker rather than assuming a fixed row count), so adding a 6th config row required no changes there — only `createSurveySheet_`'s hardcoded row numbers (used purely at sheet-creation time) and the `_SURVEY_CONFIG_ROWS` fallback-length constant needed updating. This is exactly the flexibility the marker-based design was intended to provide.
- `<details>/<summary>` is a better default than a CSS/JS tooltip for any "extra info, not always needed" UI element in a project with no client-side framework — it works with zero JS, handles touch and pointer input identically, and degrades gracefully (still readable if CSS fails to load).

### Deferred / Manual:
- User should verify the new card-based admin list and the Info disclosure render correctly on an actual mobile device/narrow viewport, and confirm the "View Survey" button opens the correct survey.

### Correction (same session, caught before shipping):
The original version of this entry claimed existing survey sheets would "self-heal" the new Info row on first edit via `writeSurveyConfig_`'s column-A scan. That was checked against the actual implementation and found FALSE: `writeSurveyConfig_` only ever *updated* a row whose key already existed in column A — it had no code path to insert a row for a key that wasn't present yet, so saving Info on a pre-existing (5-config-row) survey would have silently discarded the value forever, with no error surfaced anywhere. Fixed `writeSurveyConfig_` (`SurveyModel.js`) to append any update key with no existing row: it first reuses a blank config-block spacer row if one exists (the row between the last real config key and the Results marker), falling back to `insertRowsBefore(resultsRow, 1)` only if no spare blank row is available — reuse-first avoids old sheets accumulating a stray blank line every time a new config field is introduced later. Added `insertRowsBefore` to `test/fakeGas.js` (was missing; implemented as `insertRowsAfter(beforeRow - 1, n)`) and two new unit tests in `test/test_survey_model.js` covering both branches: `testWriteSurveyConfigSelfHealsMissingInfoRowOnOldSheet` (blank spacer present → reused, Results/Responses untouched) and `testWriteSurveyConfigInsertsRowWhenNoBlankSpacerExists` (no spacer → new row inserted, Results/Responses/header shift down by one). Redeployed to SIT (@11).

### Key Learnings (addendum):
- Caught a documentation-vs-implementation mismatch by re-reading the actual function before finalizing the work-log entry, rather than assuming the doc-string's claim ("keys are matched... tolerates reordering") implied insert-on-missing-key behavior — it didn't; the function only ever updated, never inserted. Worth specifically re-verifying "the old data will just pick up the new field automatically" type claims against the actual code path, since these silent-data-loss bugs produce no error and are easy to miss until a user reports "my notes didn't save."

## 2026-07-06 (later same day, continued)

### Summary:
User asked for two more things: (1) the ability to edit each survey item's name and add a per-item "details" note, both when editing existing items and when adding a new one, persisted in the sheet; (2) visual highlighting of the section-marker rows (Results/Items/Responses) in the raw Google Sheet for easier scanning.
- Added a new **Items** section to the sheet layout (`SurveyModel.js`), between Results and Responses: an `Items` marker row, a `Name | Details` header, then one row per candidate — **position-aligned** with the Responses header's candidate columns (item i's row == candidate column i). The Responses header remains the sole authority for RCV/Condorcet analysis (column position is what response rows key ranks off of); the Items table is a second, position-aligned view that adds the Details note, kept in sync only through model functions (never hand-edited).
  - `createSurveySheet_` skeleton grew by 2 rows (Results row8, Items marker row9, Items header row10, Responses marker row11, Responses header row12 — was 8/10/11).
  - Refactored the old `writeSurveyResults_`'s insert/delete-between-two-markers logic into a shared `_resizeSectionRows_(sheet, afterRow, beforeMarkerRow, rows)` primitive, used by both `writeSurveyResults_` (Results↔Items) and the new `writeSurveyItemDetails_` (Items header↔Responses).
  - `addSurveyCandidate_` (both the respondent-facing add-candidate path and the new admin add-item path) now takes an optional `details` param and keeps the Items table in sync — backfilling any pre-existing candidates that predate the Items table (or predate having their own Items row) with blank details first, so a newly-added item's details never land on the wrong row.
  - `saveSurveyItems_` (new): admin-facing batch rename — given a full ordered `{name, details}` array matching the survey's current candidate count, overwrites every candidate's header-row name AND its Items row in one pass. Renaming is purely positional (column index unchanged), so existing response ranks are never disturbed. Throws on a count mismatch (stale form) or an empty name.
  - `_ensureItemsSection_` (new): a survey created before this feature existed has no Items marker — this inserts an empty one (2 rows, directly above the Responses marker) the first time anything touches the Items section, so older surveys self-heal instead of throwing "missing markers." Called defensively at the top of `writeSurveyResults_`, `readSurveyItemDetails_`, and `writeSurveyItemDetails_` (not just at one call site) so correctness doesn't depend on caller discipline.
  - `_highlightSectionMarkers_` (new): bolds + light-blue-backgrounds (`#e8f0fe`) the Results/Items/Responses marker cells and the Items/Responses header rows. Called once at sheet-creation time, and again (idempotently) every time an admin opens a survey's edit page — so pre-existing sheets pick up the highlighting the next time they're visited, the same self-heal pattern used for Info/Items.
- **Admin UI** (`webAdmin.js`): added an "Items" form to the bottom of the edit page — one Name+Details field pair per existing candidate (rename/re-describe in place) plus an "Add a new item" Name+Details pair, all submitted together as one `saveItems` form action so a multi-item edit round-trips as a single sheet write. New `_handleAdminSaveItems_` dispatches to `saveSurveyItemsForId_` (rename/details batch) then `addSurveyItemForAdmin_` (if a new item name was given). Both are locked the same way as the existing respondent-facing RPCs (`LockService`), since admin edits and respondent submissions/candidate-adds must not interleave.
- Added `setBackground`/`setFontWeight` (no-op-but-chainable) to `test/fakeGas.js`'s `FakeRange` so the highlighting calls don't throw under the unit-test harness.
- Added 6 new unit tests to `test/test_survey_model.js` covering: position-aligned details tracking on add, rename+details via `saveSurveyItems_` with response-rank preservation verified, count-mismatch rejection, empty-name rejection, the `*ForId_`/`*ForAdmin_` wrapper functions, and the `_ensureItemsSection_` self-heal + backfill-on-add path for a pre-existing sheet. Updated the skeleton-row assertions for the two new rows. All pass; full `npm test` (4 suites) green.
- Deployed to SIT (@12) and verified live, end to end, via three different paths: (a) `callWebapp.js createSurvey`/`getSheet` — confirmed the new 12-row skeleton with Items marker+header; (b) a real respondent-facing `?cmd=survey&action=add` HTTP call — confirmed two added candidates landed correctly in both the Responses header and the position-aligned Items table; (c) a raw `curl` GET simulating the admin `saveItems` form submission — confirmed rename (Alice→Alicia, Bob→Robert) + details + a third new item (Carol) all landed correctly, and the Responses header columns renamed in place. Test survey sheet deleted after verification.

### Key Learnings:
- The FIRST `createSurvey` call issued immediately after a fresh `npm run deploy:sit` returned the OLD (pre-Items) sheet layout even though `getDiagnostics` already reported the new `appVersion` — a second, otherwise-identical call moments later returned the correct new layout. This looks like a brief Apps Script execution-environment propagation delay right after a deployment update (the new code version takes a few seconds to become the one actually served, even though the deployment's URL/metadata already reflect it) rather than any bug in the deployed code itself. Worth a short pause (or a retry) before trusting the very first live call right after a deploy.
- Keeping the Responses header as the sole positional authority (rather than trying to make the Items table authoritative and rebuild the header from it) avoided any risk to existing response data — renaming/adding only ever appends or edits in place, never reorders or renumbers a column, so ranks recorded against a column index stay valid no matter how many times items are renamed.
- The admin Items-save form uses `method="get"` (consistent with every other form on this page) — for a survey with many items and lengthy Details text, this could in principle approach typical URL-length limits. Not hit in practice yet; if it ever becomes a real problem, the fix is converting that one form to `method="post"` and extending `doPost` to also accept form-urlencoded (not just JSON) bodies for cmd=admin, which was deliberately deferred rather than done speculatively.

### Deferred / Manual:
- User should verify the Items edit form (rename/details/add) in an actual browser, on both desktop and mobile, and confirm the section-marker highlighting is visible on both newly-created and pre-existing (self-healed) survey sheets.
- The URL-length caveat above (admin Items form) is noted but not mitigated — revisit only if a survey with many long-Details items actually hits it.

## 2026-07-06 (later same day, continued again)

### Summary:
User identified a real, serious bug in the just-shipped Items feature: `_findMarkerRow_` does an unscoped top-to-bottom scan of column A for exact marker text ("Results"/"Items"/"Responses"), but the Items table's Name column also lived in column A — so a survey item literally named "Responses" (which sits structurally *above* the real Responses marker, since the Items section precedes it) would shadow the real marker and corrupt every section boundary computed from it. User suggested two fixes: decorate the marker text to make it distinctive, and/or reserve column A for section identifiers only, never user data. Implemented both, as defense in depth.
- **Column A reservation**: moved the Items table's Name/Details columns from A/B to B/C (`_SURVEY_ITEMS_FIRST_COL = 2`) — column A is now blank throughout the entire Items section (marker row excepted). `createSurveySheet_`, `_ensureItemsSection_`, `readSurveyItemDetails_`, and `writeSurveyItemDetails_` updated to read/write at the new column offset. Generalized `_resizeSectionRows_` with an optional `startCol` param (defaults to col A, used by `writeSurveyResults_`'s Results-section content, which is app-generated report text and has no collision risk) so the Items-section caller can pass `_SURVEY_ITEMS_FIRST_COL` explicitly.
- **Marker decoration**: `_SURVEY_RESULTS_MARKER`/`_SURVEY_ITEMS_MARKER`/`_SURVEY_RESPONSES_MARKER` changed from bare `'Results'`/`'Items'`/`'Responses'` to bracket-wrapped `'[Results]'`/`'[Items]'`/`'[Responses]'`. `_findMarkerRow_` now also recognizes the old bare text via a `_SURVEY_LEGACY_MARKER_TEXT_` lookup table — the moment a legacy-text marker is found, the cell is rewritten to the decorated form in place, so existing sheets (including live ones on SIT) migrate to the new markers automatically and exactly once, the same self-heal pattern used for Info/Items. This migration is safe precisely because legacy (pre-Items-table) sheets never had item data in column A to begin with — the vulnerability only existed for sheets that had already gone through the Items-table feature, which shipped baldy in this same session.
- `_highlightSectionMarkers_`'s Items-header highlight span updated to still cover a continuous band from col A (blank) through the end of the Details column, so the visual highlighting still reads as one continuous bar despite col A being empty there.
- Updated all affected `test/test_survey_model.js` assertions (decorated marker text, Items header/data now at col B, legacy-migration expectations for the two config self-heal tests — Results migrates when looked up, Responses does not since those two tests never look it up). All still pass; full `npm test` green.
- Deployed to SIT (@13) and ran the **exact attack scenario** live: created a survey, then via the real respondent-facing add-item endpoint added items literally named "Responses" and "Items" (plus a normal "Alice"), confirmed via `getSheet` that the real markers stayed correctly at `[Results]`/`[Items]`/`[Responses]` in column A while the adversarial item names landed safely in column B with no corruption — then submitted a ballot and ran a full RCV+Condorcet analysis (via a raw `curl` GET simulating the admin analyze click) and confirmed it correctly treated "Responses"/"Items"/"Alice" as ordinary candidate names throughout (RCV winner correctly reported as "Responses" the candidate, Ranked Pairs winner also "Responses", etc. — no confusion with the real section markers anywhere). Test sheet deleted after verification.

### Key Learnings:
- A structural/positional argument ("items always sit *below* the real Results marker, so they can't shadow it") is not a sufficient guarantee against a scanning bug — it only holds for TWO of the three markers (Results is genuinely safe this way, since nothing sits above it in-section); Items and Responses are each preceded by exactly the section whose content is user-controlled, which is what actually made the exploit possible. When reasoning about whether a scan-based lookup is safe from collision, check each marker's position independently rather than assuming a single "user content is below" mental model applies uniformly.
- Combining "reserve the scanned column for metadata only" (the structural fix — closes the hole entirely for all future data) with "decorate the sentinel text" (defense in depth — the fallback if a future column-A mistake is ever reintroduced, or if the sheet is hand-edited outside the app) gives two independent guarantees rather than relying on either alone; this mirrors the file's other established pattern (in-place self-healing migration) rather than requiring a one-time manual fix-up script.
- A transient ~2-minute `curl` timeout while live-testing turned out to be unrelated to the fix (a retry with `--max-time 30` succeeded in ~5 seconds) — worth re-testing before treating a single slow/hung request as a real regression, especially against a `script.google.com` execution URL which can have variable cold-start latency.

### Deferred / Manual:
- User should confirm the section-marker highlighting and Items form still look correct in the browser after this column shift (col A is now visibly blank in the Items section, by design).

## 2026-07-06 09:36:07

### Summary:
Full-session recap (detailed entries for each sub-task already logged above under earlier same-day headers): upgraded RankChoiceVoting's webapp/admin/test framework to the F3Go30 model — rewrote `GasLogger.js` with an Axiom sink + Drive fallback, added a minimal gated admin JSON API (`WebApp.js`: bootstrapSecret/setScriptProperties/setWebappUrl/listSheets/getSheet/setSheet/deleteSheet/createSurvey/getDiagnostics), built out `tools/callWebapp.js`, `tools/query-axiom.js`, `tools/smokeTest.js`, and a Node unit-test suite (`test/fakeGas.js` + 4 suites) wired into `npm test`. Diagnosed and fixed the reported "blank page after creating a survey" bug (root cause: HtmlService sandbox navigation needs both `target="_top"` AND an absolute URL, not a relative one — found via a user-supplied HAR trace). Fixed `WEBAPP_URL` never being stamped for SIT deploys (menu item pointed at the wrong URL). Added mobile-responsive card layout + touch targets to the admin page and survey page, a "View Survey" link per list entry, and a per-survey admin-only "Info" disclosure (`<details>`, not a tooltip — no reliable tap-trigger on touch). Added a full "Items" management feature (per-candidate Name + Details, editable via a new admin form, backward-compatible self-heal for older sheets). Then fixed a real security/correctness bug the user caught: section markers (Results/Items/Responses) were found via an unscoped column-A text scan that a user-named item (e.g. one literally named "Responses") could shadow — fixed by reserving column A for markers only (Items table data moved to column B+) and bracket-decorating the marker text (`[Results]` etc.), with automatic in-place migration for existing sheets. Everything verified live against a real SIT deployment (currently @13) via `tools/callWebapp.js`, including an explicit adversarial test naming items after the marker keywords themselves.

### Key Learnings:
- Apps Script's HtmlService sandbox requires BOTH `target="_top"` and an absolute URL for any in-page form/link meant to load a new server-rendered page — either alone is insufficient, and a passing server-side/API test does not prove the browser-rendered path works, since the sandbox layer only affects real browser navigation.
- Adding explicit `oauthScopes` to `appsscript.json` requires a fresh interactive authorization (a function that actually exercises the new scope, run from the editor) — Apps Script's implicit auto-granted scopes don't carry over, and a deployed anonymous web app can't prompt for consent itself.
- When a scan-based lookup (marker text in column A) shares a column with user-controlled data, prefer removing the column overlap entirely (structural fix) over just making the sentinel value "unlikely to collide" (probabilistic fix) — do both when the cost is low, as was done here.

### Note:
Cache context has grown large this session — consider running `/clear` to start fresh for future work.

## 2026-07-06 (continued — respondent-facing details gap)

### Summary:
User asked where to see item details while viewing/ranking a survey — pointed out a real gap: the "Details" field added earlier this session was wired into the admin edit page only; `getSurveyForRespondent_` never included it, so it was invisible to actual respondents, which defeats the point of the feature (voters, not just admins, should be able to see more context about what they're ranking).
- `SurveyModel.js`: `getSurveyForRespondent_` now builds an `itemDetails` object (name → details, omitting empty ones) from `readSurveyItemDetails_` and includes it in the RPC payload. Keyed by name rather than position so it stays correct even though `candidates` in the same payload gets reordered per-respondent to match their saved ranking.
- `webSurveyPage.html`: `applyBranding` now captures `config.itemDetails` into a module-level `itemDetailsByName` map. `buildItem()` (the per-candidate `<li>` builder used by both the initial render and the "add new item" flow) now appends a `<details><summary>ℹ️ Details</summary><p>...</p></details>` disclosure when the item has a non-empty entry — same tap-friendly pattern as the admin list's "More info", intentionally not a tooltip. Restructured the `<li>` from a single flex row into a flex column containing a `.topic-row` (handle + text) plus the optional details block below it, so the disclosure doesn't have to squeeze into the drag-handle row. Items added by respondents (who can't set details) simply have no entry and render with no disclosure — no special-casing needed.
- Added `testGetSurveyForRespondentIncludesItemDetails` to `test/test_survey_model.js`. All 4 suites pass. Deployed to SIT (@14) and verified live: added an item, set its details via the admin `saveItems` action, then confirmed the `?cmd=survey&action=config` RPC response includes `itemDetails` correctly.

### Key Learnings:
- When a new per-item field is added to the model, check every consumer of that model, not just the one UI surface the request explicitly named — "editing a survey" (admin) and "viewing a survey" (respondent) are two different RPC payloads (`getSurveyForRespondent_` serves both, called with/without a name), and it's easy to wire a new field into the admin path while forgetting the respondent path reads through the exact same function but was never actually returning the new field to itself further down.

## 2026-07-06 (continued — mobile tap on item details didn't work)

### Summary:
User confirmed the "no details visible on mobile" issue with the real live survey (`Survey-ABC-2026-7`) — inspecting it via `getSheet` confirmed the server side was correct (book 1/2/3 all have real Details text set, and would have been returned to the client via `getSurveyForRespondent_`), so the bug was client-side and mobile-specific. Root cause: `ul#topicList` sets `touch-action: none` (required so drag-reordering doesn't trigger page scroll/zoom while dragging) — that suppresses tap/click gestures for everything nested inside it on many mobile browsers (notably iOS Safari), including the new `<details>` disclosure, unless a descendant explicitly opts back in to normal tap handling. Fixed by adding `touch-action: manipulation` to `.topic-info` and `.topic-info summary` in `webSurveyPage.html`. Deployed to SIT (@15).

### Key Learnings:
- `touch-action: none` on a container isn't purely about scroll/zoom prevention — it can silently suppress normal tap-to-activate behavior (e.g. a `<details>` toggle) on nested interactive elements on some mobile browsers, with no console warning or visible error. Any new tappable element added inside an existing `touch-action: none` region needs its own explicit `touch-action: manipulation` (or `auto`) to opt back in — this is easy to miss because it works perfectly in desktop testing (mouse click) and only fails on an actual touch device.

### Flagged, not acted on:
- `Survey-ABC-2026-7`'s Items table has a 4th entry, "Adding a new item" (no Details set), with real recorded votes from two respondents (LJ, Stu, both ranked it 3rd). This looks like a stray test candidate — possibly someone typed literal placeholder-like text into an add-item field while testing rather than a real candidate name. Left alone since it has real vote data attached (renaming/removing would need to be a deliberate user decision, not something to silently correct); flagged to the user for one of the deferred/manual items.

## 2026-07-06 (continued — item details still missing on the actual drag/rank page)

### Summary:
User reported details still weren't showing on the actual mobile drag-to-rank page (not just "hard to tap" — the earlier touch-action fix didn't help). Checked the live RPC payload directly for a real respondent (`?cmd=survey&action=data&id=ABC-2026-7&name=LJ`) and found `itemDetails: {}` — completely empty, even though the sheet's Items table clearly has real Details text for book 1/2/3. Root cause: `Survey-ABC-2026-7`'s Items section was created/populated *before* the earlier column-A-reservation fix shipped, so its Name/Details data still physically sits in columns A/B. The current `readSurveyItemDetails_` reads from columns B/C (per the new layout) — on this sheet that silently reads the OLD Details value as if it were the Name, and an always-empty column C as Details, producing zero non-empty entries. This is exactly the kind of bug the earlier `_ensureItemsSection_` self-heal was designed to prevent, but it only handled a *missing* Items section — not an *existing* one still on the old column layout.

Fixed by adding `_migrateItemsColumnsIfNeeded_(sheet)` (`SurveyModel.js`): detects the old layout by checking whether the Items header's column A cell still holds the literal "Name" label (the new layout never writes anything to column A there), and if so, reads the old A/B data, blanks column A, and rewrites header+data at B/C. Called from both `readSurveyItemDetails_` and `writeSurveyItemDetails_`, so it self-heals automatically on next touch — same pattern as every other migration this session. Added `clearContent()` to `test/fakeGas.js`'s `FakeRange` (was missing) and a new unit test, `testMigratesExistingOldColumnItemsSectionOnRead`, that reproduces the exact live scenario (Name/Details in columns A/B, book 1/2/3 with real details). All 4 test suites still pass. Deployed to SIT (@16) and confirmed live against the actual affected survey: `itemDetails` now returns correctly for a real respondent, and inspecting the raw sheet confirmed the migration landed cleanly (column A now blank, data intact at B/C) with all vote data and the Responses header completely untouched.

### Key Learnings:
- A "self-heal on next touch" migration strategy is only as complete as the set of *prior states* it accounts for. This session shipped two different Items-related self-heals (missing section entirely, then wrong column layout) as two separate incidents rather than one — worth remembering, next time a stored-data layout changes mid-project, to explicitly enumerate "does this sheet already have partial/older-format data for the thing I'm changing?" as its own checklist item, not just "does it have the section at all?"
- When a client-visible field comes back empty/wrong, checking the actual live RPC payload (not just the admin API or a synthetic test sheet) was what actually found this — the synthetic `test_survey_model.js` coverage and the earlier "DetailsCheck" live smoke test both used *freshly created* sheets, which never exercised the "existing sheet from before this feature" code path at all. A synthetic test for a migration needs to specifically construct the OLD state, not just verify the NEW state works.

## 2026-07-06 (continued — condense the details toggle)

### Summary:
User found the `<details>`-based disclosure took an extra visible line per item even when collapsed, and asked for a more condensed layout: an info icon on the right of the row, or the item's title itself being clickable — both, if practical.
- Replaced the native `<details>/<summary>` per item with a small `.info-toggle` icon button (ℹ️) appended to the same `.topic-row` as the handle and name (flex layout pushes it to the right automatically, since the name text has `flex:1`), plus made the item name text itself clickable too (`.topic-text.has-details`, larger/easier tap target on mobile) — both call the same toggle function. The details content is a plain `<div class="topic-info-content" hidden>` appended as a sibling after the row, so it only takes up space once actually expanded; collapsed items are back to a single compact row. Kept the earlier `touch-action: manipulation` fix on the new interactive elements (still needed since the parent `ul#topicList` sets `touch-action: none` for drag-reordering). Items with no details render with neither the icon nor a clickable-cursor name — unchanged from before.
- No server-side changes needed (purely `webSurveyPage.html`); `npm test` unaffected/still green. Deployed to SIT (@17) and confirmed the served page HTML for the real `ABC-2026-7` survey includes the new `info-toggle`/`topic-info-content`/`has-details` classes.

### Deferred / Manual:
- User should confirm on their mobile device that the list is now visibly more compact (no line taken by collapsed items) and that both the icon and tapping the item name toggle the details correctly.

## 2026-07-06 (continued — scrolling broken past ~6-7 items)

### Summary:
User reported the survey page became unusable with more than 6-7 items (or fewer, if item names/details were long) — page scrolling appeared disabled entirely, so items further down the list were unreachable. Root cause: `ul#topicList` had `touch-action: none` applied to the WHOLE list (not just the drag handle), which blocks all vertical scroll gestures anywhere over the list, at all times — not just during an active drag. Fixed by removing `touch-action: none` from `ul#topicList`, keeping it only on `.handle`. This is sufficient because touch-action is resolved once, at the start of a touch gesture, based on the element the finger first lands on — a drag always starts on `.handle`, so `.handle`'s own `touch-action: none` still fully protects an in-progress drag (even once the finger moves off the handle mid-gesture) without needing to disable scrolling for the rest of the list. `npm test` unaffected (pure CSS change). Deployed to SIT (@18) and confirmed via the served page HTML that only one `touch-action: none` declaration now ships (on `.handle`), down from two.

### Key Learnings:
- `touch-action: none` should be scoped to the *smallest* element that actually initiates the gesture needing protection (here, the drag handle), not a whole containing list/section "to be safe" — CSS touch-action resolves from the gesture's start point, so over-scoping it doesn't add safety margin, it just disables normal touch behavior (scrolling) everywhere under that ancestor, which is what broke this.

## 2026-07-06 (continued — mobile screenshot feedback batch)

### Summary:
User sent a mobile screenshot showing several issues on the survey ranking page: (1) short item names like "book 2" wrapping onto two lines, but only when the info icon was present; (2) excessive vertical whitespace between items; (3) "Comments" label should read "Feedback"; (4) the "Add" button should become "Add New" and open a proper view for entering both a name and details, with newly-added items assumed to rank at the top rather than the bottom.
- **Wrap bug root cause**: `webSurveyPage.html` never had a global `box-sizing: border-box` rule (only inputs/textareas had it individually) — so `.info-toggle`'s `min-width: 44px` + its own padding rendered WIDER than 44px (content-box adds padding on top of the specified width), squeezing `.topic-text` narrower than intended and causing short labels to wrap unnecessarily. Fixed with a global `*, *::before, *::after { box-sizing: border-box; }` plus `min-width: 0` on `.topic-text` (the standard flexbox fix that lets a flex item actually shrink instead of refusing to go below its content's natural width). Also corrected `.handle`'s `min-height` from `24px` to `44px`, since under border-box it now needs to specify the desired *total* touch-target size rather than relying on padding to add up to it incidentally.
- **Whitespace**: tightened `ul#topicList li` padding (12px→8-10px) and margin-bottom (8px→6px), and the list's own top margin (16px→12px).
- **"Comments" → "Feedback"**: relabeled the field and its placeholder; left the `commentInput` id/variable name unchanged (internal, no observable effect).
- **"Add New" flow**: replaced the always-visible inline name-input-and-Add-button row with a collapsed "+ Add New" button that reveals an `.add-panel` (Name input + Details textarea + Add/Cancel buttons) — matching the admin-side Items form's shape, now available to respondents too. Extended `addSurveyCandidateForId_` (`SurveyModel.js`) and its RPC wrapper `addSurveyTopic` (`webSurvey.js`) with an optional `details` param (previously admin-only), and fixed the GET-fallback diagnostic dispatcher (`_handleSurvey`'s `action=add` case) to pass it through too — the real browser client calls `addSurveyTopic` directly via `google.script.run` so this particular fix doesn't affect end users, but it matters for `curl`/tooling-based testing of the same path. New items now insert at the TOP of the client-side ranking list (`topicList.insertBefore(..., topicList.firstChild)`, was `appendChild`) and their details are registered into `itemDetailsByName` immediately so the just-added item's own info icon works without a page reload.
- Added `testAddSurveyCandidateForIdAcceptsRespondentDetails` to `test/test_survey_model.js`. All 4 suites pass. Deployed to SIT (@19, then @20/@21 for the dispatcher fix) and verified live: the served survey page includes "Add New"/"Feedback"/the new panel markup, and a `curl`-driven add-with-details round-trip correctly persisted both the name and details into the Items table.

### Key Learnings:
- A missing global `box-sizing: border-box` combined with a fixed `min-width` on one flex sibling is a classic, easy-to-miss cause of "this only wraps when X is present" bugs — content-box sizing means padding is added on top of any explicit width/min-width, silently consuming more layout space than the CSS declares.
- Respondent-visible copy ("Comments" vs "Feedback", "Add" vs "Add New") is worth treating as a first-class review item, not an afterthought — small wording choices affect how contributors perceive whether their input is welcome.

## 2026-07-06 (continued — wrap bug persisted after the box-sizing fix)

### Summary:
User sent another screenshot showing the exact same "book 2" wrapping bug, apparently unresolved after the box-sizing/min-width fix from the previous round. Decoded the actual live-served page HTML directly (extracted and unescaped the `goog.script.init(...)` payload from a raw `curl` fetch) to confirm the fix genuinely was deployed and being served — it was (`box-sizing: border-box` and `.topic-text{min-width:0}` both present). So the theoretical pixel budget (handle + gaps + a 44px icon button should leave ~200px+ for "book 2" on a typical phone width) said this shouldn't wrap, but real mobile rendering (likely emoji glyph metrics, native button chrome, or device-specific scaling not accounted for in the calculation) was still consuming more width than expected. Rather than keep refining an increasingly fragile pixel-budget guess, switched to the standard bulletproof fix: `.topic-text` now has `white-space: nowrap; overflow: hidden; text-overflow: ellipsis`, forcing a guaranteed single line regardless of available width — short names like "book 2" are unaffected either way, and only a genuinely very long name would ever truncate (with the full text still reachable via the details toggle for items that have one). Deployed to SIT and confirmed the new rule is present in the actual served page.

### Key Learnings:
- When a CSS layout bug survives a theoretically-correct fix, verify what's ACTUALLY being served (decode the live page, don't just trust "I deployed it") before concluding the fix didn't work — in this case the fix was live, the THEORY about why it would work was just wrong (real device rendering has more variables — emoji glyph width, native form-control chrome, device pixel ratio — than a manual pixel-budget calculation accounts for). When the theory and reality disagree twice in a row, stop refining the theory and switch to the version of the fix that doesn't depend on the theory being right (nowrap+ellipsis instead of "make sure there's enough room").

## 2026-07-06 (continued — text collapsing to 0 width, icon not flush right)

### Summary:
User inspected the page in DevTools and found `.topic-text`'s computed width was effectively 0 (not just the `min-width:0` I'd set intentionally) — the text was still clickable/functional but visually hidden, and the info icon sat short of the right edge rather than flush against it. Both symptoms point to the same root cause: `flex-grow:1` on `.topic-text` wasn't actually distributing the row's remaining space to it in practice, despite the CSS being textbook-correct (`auto | flex:1 | auto` is normally a reliable flexbox pattern). Rather than keep debugging why flexbox wasn't behaving as expected on this device/browser, switched `.topic-row` from `display:flex` to `display:grid` with `grid-template-columns: auto 1fr auto` — a well-supported, less ambiguous way to express the same "fixed | fill-and-truncate | fixed" layout, which doesn't depend on flex-grow distribution working correctly among whatever's in the middle column. Still works with only 2 children (no icon) — the unused third grid track collapses to 0 width automatically. Kept `min-width: 0` on `.topic-text` (Grid has the same "don't shrink below content" default as flexbox for track items) and the `white-space:nowrap`/ellipsis truncation from the previous fix. Removed the now-irrelevant `flex: 0 0 auto` from `.info-toggle` (flex properties don't apply to grid items). Deployed to SIT (@22) and confirmed the new `display: grid; grid-template-columns: auto 1fr auto` rule is present in the live served page.

### Key Learnings:
- Two fixes in a row (box-sizing, then nowrap+ellipsis) targeted "why is there not enough width," but the user's DevTools inspection revealed the real issue was "the text isn't growing to fill the width that IS there" — a completely different failure mode (flex-grow not distributing) than what both prior fixes addressed (available width being smaller than expected). This is a reminder to ask for or obtain a DevTools computed-style/element-inspector view earlier when a CSS layout bug survives more than one plausible fix — it turns "guess what's wrong" into "read what's actually wrong."
- CSS Grid's `auto 1fr auto` template is a more robust choice than flexbox's `auto | flex:1 | auto` for a "fixed-left, fill-and-truncate-middle, fixed-right" row layout — it doesn't rely on flex-grow distribution succeeding, worth defaulting to Grid for this specific shape of layout rather than flexbox.

## 2026-07-06 13:54:06

### Summary:
Session recap covering the mobile-UI debugging arc since the last full-session summary (detailed blow-by-blow entries for each already logged above): the "item details" feature shipped earlier this session turned out to need several follow-up fixes once tested on a real mobile device against the live SIT survey. In order: (1) found and fixed an existing survey whose Items table was still on the pre-column-A-reservation layout, causing `itemDetails` to come back empty for real respondents — added `_migrateItemsColumnsIfNeeded_` (`SurveyModel.js`) to self-heal that in place; (2) fixed `touch-action: none` being scoped to the entire item list instead of just the drag handle, which had silently disabled all scrolling past ~6-7 items; (3) condensed the details disclosure from an always-visible `<details>` summary line into a compact inline icon + tappable item name, expanding only when opened; (4) fixed short item names ("book 2") wrapping onto two lines whenever the info icon was present — first via a global `box-sizing:border-box` fix, then a `white-space:nowrap`+ellipsis fallback, and finally (after the user's own DevTools inspection showed `.topic-text` was computed to ~0 width and the icon wasn't flush right) by replacing the row's flexbox layout with CSS Grid (`auto 1fr auto`), which doesn't depend on flex-grow distributing space correctly the way flexbox was failing to; (5) relabeled "Comments" to "Feedback"; (6) replaced the always-visible inline add-item row with a collapsible "+ Add New" panel supporting both a Name and a Details field (previously Details was admin-only), with newly-added items now inserted at the top of the ranking instead of the bottom. Every fix was deployed to SIT and verified against the live, real survey (not just synthetic test data) before being reported back.

### Key Learnings:
- Real mobile-device testing surfaced multiple distinct bugs that no amount of local reasoning would have caught: a pre-existing sheet still on an old column layout, `touch-action` scoping, and a flexbox layout that didn't behave as its (textbook-correct-looking) CSS suggested it should. Each required either live data inspection (`callWebapp.js getSheet`) or the user's own DevTools output to actually diagnose — confirms the pattern from earlier in this session that server-side/API verification is necessary but not sufficient for anything touching the browser-rendered UI.
- When a CSS fix survives one plausible theory and the bug persists, get (or ask for) the actual computed-style/DevTools evidence before trying a second theory — the flexbox debugging round-tripped twice on plausible-but-wrong theories (available width, then forced single-line) before the user's own inspection revealed the real failure mode (flex-grow not distributing), which prompted switching to Grid instead of continuing to patch flexbox.

Cache context has grown large this session — consider running `/clear` to start fresh for future work.

## 2026-07-06 18:31:23

### Summary:
Completed bead RankChoiceVoting-5mo: removed Google Forms-era code (createRCVform.js, cleanResponses.js, testRCV.js), the Forms-only wrapper functions processRankedChoiceVotes()/processCondorcetVoting(), and the corresponding onOpen.js menu items, keeping the underlying RCV/Condorcet algorithms since webAdmin.js calls them directly with survey-sourced data. Renamed the survey's "[Items]" sheet section to "[Candidates]" and made it the primary source of candidate identity — readSurveyCandidates_ now reconciles the Responses header against the Candidates table on every read, appending columns for any pre-populated candidate not yet in Responses, without disturbing existing columns/ranks. Added legacy-marker migration ("[Items]"/"Items" -> "[Candidates]"). Also declared the missing Drive oauth scope in appsscript.json for GasLogger.js's active Drive-backed logging sink (DriveApp.getFolderById on an arbitrary pre-existing folder needs the full drive scope, not drive.file). Committed and pushed several rounds of changes, including prior pending NUUC deployment tooling work, and deployed to NUUC multiple times (up to v0.0.4) for live testing.

### Key Learnings:
- `clasp push` does a full remote-content replacement, not an additive sync — deleting a file locally and running `clasp push -f` removes it from the live Apps Script project automatically. Confirmed this directly by pulling the NUUC project's remote file list into a scratch dir rather than assuming from clasp's behavior alone.
- When a feature audit says "no code changes needed" for a file (e.g. GasLogger.js's Drive usage), that's a statement about whether the code is dead, not whether its configuration (oauth scopes) is correct — worth checking those separately, as the appsscript.json manifest here was missing the Drive scope that DriveApp.getFolderById() on an arbitrary pre-existing folder ID actually requires (drive.file wouldn't have covered it).

## 2026-07-06 19:56:53

### Summary:
Extensive survey/ballot UX session across SurveyModel.js, webAdmin.js, webSurveyPage.html, and a new webAdminEditPage.html. (1) Split the single "Description" config field into a landing-page intro (Description) shown before name entry and a separate "Instructions" field shown on the ballot page instead, replacing it there; added "Add-Instructions" text shown above the respondent "+ Add New" button. (2) Made the survey list show candidate count alongside response count, and added `findRespondentsWithNewCandidates_` (SurveyModel.js) to flag respondents whose last submission predates a since-added candidate, surfaced as a disclosure in the admin list. (3) Renamed the "Info" config key to "Admin-Only-Notes" with legacy-key migration in `readSurveyConfig_` (same pattern as marker migration). (4) On the respondent ballot page: made Feedback a collapsible "+ Add Feedback" panel (matching "+ Add New"), moved it to the very bottom of the page, and moved "+ Add New" to after Submit/Back. (5) Fully replaced the admin "Edit Survey" page: it used to be a static server-rendered form with a page-wide Save; it's now `webAdminEditPage.html`, a live preview styled exactly like the real ballot (Landing Page section immediately followed by Ballot Page section, matching dividers), with a pencil icon on every editable field that opens an inline editor and saves immediately via per-field `google.script.run` RPCs (`adminSaveTitle/Description/Instructions/Footer/Contact/AdminOnlyNotes/AddSettings`, `adminSaveCandidate`, `adminAddCandidate` — all new in webAdmin.js) — no page-wide Save, only Back. Fixed page copy (name-entry prompt, drag hint, Submit/Back, "+ Add New", "+ Add Feedback") is now also shown in the preview, visually marked as non-editable (dashed border + "Fixed text" label) so admins don't try to re-add wording that's already there; Admin-Only Notes got its own visually distinct warm-toned box. (6) While building the preview, discovered and fixed a real bug: the "Contact" config field was being collected by the admin form but never actually rendered anywhere on the live respondent ballot — added it near the footer in webSurveyPage.html.

### Key Learnings:
- When asked to build an admin "preview that shows full context," take the "full context" requirement literally: fixed/static page copy needs to be visible in the preview too, not just the editable fields, and it needs a distinct visual treatment (dashed border, "not editable" label) so it doesn't look like a field that's simply blank. Confirmed via iteration — first pass only showed editable fields, which prompted this correction.
- Building the accurate preview is also a good forcing function for catching drift between "what the admin edits" and "what respondents actually see" — it directly surfaced a real bug (Contact never rendered) that a form-only admin UI had been quietly hiding.
- GAS's `HtmlService.createHtmlOutputFromFile` + `google.script.run` per-field RPCs is the right shape for "save immediately, no page reload" admin UIs — the previous multi-field `<form method="get">` approach couldn't support that without a full rearchitecture, which is what this session's edit-page rewrite did.

## 2026-07-06 20:19:04

### Summary:
Fixed confusing UI in admin edit page's "Add New Candidates" section (script/webAdminEditPage.html), where the accept-new-candidates on/off status and the configured instructional text were mashed into a single sentence. Split into two separate display lines: a status line (`addSettingsDisplay`) and a distinct instructions line (`addInstructionsDisplay`) that only shows when instructions text is configured.

### Key Learnings:
None.

## 2026-07-06 20:46:05

### Summary:
(1) Added unique-respondent counting to fix a latent double-counting risk: a respondent re-voting always overwrote their existing row in place, but nothing enforced or displayed that "only the most recent vote counts" rule elsewhere. Added `_latestResponseByRespondent_` (shared dedup helper, case-insensitive by name) and `countUniqueSurveyRespondents_` in SurveyModel.js; reused the helper in `findRespondentsWithNewCandidates_`; and applied it in `runSurveyAnalysis_` (webAdmin.js) so RCV/Condorcet ballots are deduped before analysis runs — a hand-edited duplicate row can no longer count twice. The admin survey list now shows both counts: "12 responses (11 unique respondents)". Added `testCountUniqueSurveyRespondents` (test/test_survey_model.js), which manually inserts a duplicate row (since normal submission can't produce one) to verify the dedup. (2) Fully rewrote README.md, which still described the old, fully-removed Google-Forms-era workflow — replaced with the actual Config/Results/Candidates/Responses sheet layout, the three web-app URLs (cmd=admin, cmd=admin&action=edit, cmd=survey), survey creation/config steps, the vote-overwrite-on-revote rule, and how analysis works.

### Key Learnings:
- `submitSurveyResponse_` already overwrites a respondent's existing row by case-insensitive name match, so duplicate response rows can't occur through normal use — they can only appear via a hand-edited sheet or legacy/imported data. Tests that need a duplicate row must write it directly via `sheet.getRange(...).setValues(...)`, bypassing the model's own submit path.
- READMEs need to be swept for staleness after an architecture removal (Google Forms era) even when other docs (SurveyModel.js header comments, CLAUDE.md) stay current — nothing enforces README accuracy automatically, unlike code-adjacent comments.

## 2026-07-06 21:21:13

### Summary:
Renamed the "Survey" terminology to "Ballot" throughout the codebase and docs: the poll/entity concept (`Survey-<id>` sheet, admin survey list, `cmd=survey`) is now "Ballot" (`Ballot-<id>` sheet, `cmd=ballot`). Renamed `script/SurveyModel.js`→`script/BallotModel.js`, `script/webSurvey.js`→`script/webBallot.js`, `script/webSurveyPage.html`→`script/webBallotPage.html`, `test/test_survey_model.js`→`test/test_ballot_model.js`. Updated all identifiers, RPC action names (`createSurvey`→`createBallot`), menu labels ("Voting and Ballot Tools"), README, and `local.settings.json.example`. Deliberately left `work-log.md`'s historical entries untouched (they describe past state) and left `script/processRCV.js`'s existing "ballot" usage (a single voter's ranked submission — standard election-science terminology, unrelated to the entity rename) alone, per explicit user decision to accept that pre-existing double meaning rather than rename it to "ranking". `npm test` passes all 4 suites after the rename.

### Key Learnings:
- Before a blanket terminology rename, grep for the target word's existing usage first — this repo already used "ballot" in two other senses (the respondent-facing voting page in prose, and a single voter's ranked submission in `processRCV.js`), and a blind rename would have created real ambiguity. Surfacing the collision to the user before touching code avoided a rename-and-redo.
- Mechanical case-preserving sed (`Survey`→`Ballot`, `survey`→`ballot`, `SURVEY`→`BALLOT`) is safe and sufficient for this kind of identifier+prose rename across camelCase, PascalCase, and plain text, as long as the excluded files/usages are decided first.

### Deferred / Manual:
- The real Axiom dataset name in `local.settings.json` (`axiomDataset: "surveys"`) was left unchanged — it's an external resource name, not code; only the `.example` file's comment was updated to say "ballots".

## 2026-07-06 22:18:59

### Summary:
Added optional Link Text/Link URL fields to Candidates, rendered as a new-tab hyperlink in the candidate's expanded detail view on the ballot page, with matching edit/add fields in the admin edit page. Widened the `[Candidates]` sheet section header (`BallotModel.js`) from `['Name', 'Details']` to `['Name', 'Details', 'Link Text', 'Link URL']`, added a self-healing header-backfill helper (`_ensureCandidatesHeaderColumns_`) so sheets created before this change pick up the new columns on next read/write, and threaded `linkText`/`linkUrl` through `readBallotCandidateDetails_`/`writeBallotCandidateDetails_`/`saveBallotCandidates_`/`addBallotCandidate_`/`getBallotForRespondent_` and the `adminSaveCandidate`/`adminAddCandidate` RPCs (`webAdmin.js`). Updated `webBallotPage.html`'s info-toggle to render an `<a target="_blank" rel="noopener noreferrer">` link alongside details text, and `webAdminEditPage.html`'s candidate edit/add panels to include Link Text/Link URL inputs and display the link. Updated `test/test_ballot_model.js` expectations for the new `{name, details, linkText, linkUrl}` return shape; `npm test` passes all 4 suites.

### Key Learnings:
- Widening the Candidates header (2 cols → 4) exposed a latent bug in `addBallotCandidate_`: it computed the next candidate column via `sheet.getLastColumn() + 1`, which happened to work only because the old 2-column Candidates header ended one column short of `_BALLOT_FIRST_CANDIDATE_COL` (5). With a 4-column header reaching col E, `getLastColumn()` returned 5, pushing new candidates to col 6 and leaving a blank gap in the Responses header. Fixed by deriving the next column from the existing candidate count (`_BALLOT_FIRST_CANDIDATE_COL + existingCandidates.length`) instead of the sheet-wide last column — any future column-count-sensitive logic in this file should prefer counting the relevant section's own data over `getLastColumn()`, since unrelated sections can extend further right.
- The RPC field `itemDetails` changed shape from a bare string (details only) to `{details, linkText, linkUrl}` — a client/server-only contract change (both sides are controlled in this repo), but it required updating every test assertion on `readBallotCandidateDetails_`/`itemDetails` output, not just the new fields' plumbing.

## 2026-07-07 05:40:00

### Summary:
Fixed a live data-corruption bug on the SIT `Ballot-ABC-2026-7` sheet: the Responses header's candidate-name columns were shifted one column right of the vote data beneath them (a blank gap at col E, names starting at col F but ranks at col E), with a stray blank row also left in the `[Candidates]` table. Root cause was the sibling bug to the one fixed in the previous entry's `addBallotCandidate_`, still present in `_reconcileCandidatesWithResponses_` (`BallotModel.js`): it computed the next append column from `sheet.getLastColumn() + 1`, which reflects the wider `[Candidates]` table's own fixed-width header (through its Link URL column) rather than the Responses header's own candidate count, whenever no candidate had a Responses column yet. Fixed by deriving the next column from the header's own (compacted) candidate count instead, and added a self-healing check in the same function: any blank cell found before a non-blank one in the Responses header's candidate range is now detected, logged, and compacted left automatically. Also made `readBallotCandidateDetails_` ignore rows with no candidate name, so a stray blank row in the `[Candidates]` table is never treated as a phantom candidate. Added regression tests (`testReadBallotCandidateDetailsIgnoresBlankRow`, `testReconcileCompactsGapCausedByWiderCandidatesTable`) to `test/test_ballot_model.js`; `npm test` passes all 4 suites. Deployed the fix to SIT (build 0.1.2.2) and triggered the self-heal on the live sheet via a normal ballot-page GET (`?cmd=ballot&action=config&id=ABC-2026-7`), which re-ran `_reconcileCandidatesWithResponses_` and rewrote the Responses header in place; confirmed via `getSheet` that ranks 1–6 now line up with the candidates in their listed order.

### Key Learnings:
- The fix in the prior entry (`addBallotCandidate_`) addressed only one of two call sites with the identical `sheet.getLastColumn()`-based miscalculation — `_reconcileCandidatesWithResponses_` had the same bug and was the one actually exercised by this ballot (its candidates were pre-populated directly into the `[Candidates]` table without ever calling `addBallotCandidate_`). When a bug pattern is found in one function, grep the file for the same anti-pattern (`getLastColumn()`) rather than assuming a single call site.
- Because `submitBallotResponse_` always writes vote ranks starting at the fixed `_BALLOT_FIRST_CANDIDATE_COL` regardless of the header's own state, a misaligned Responses header can be repaired in place by compacting the header row alone — the vote data itself was never actually corrupted, only mislabeled.
- Making the self-heal in `_reconcileCandidatesWithResponses_` run on every read (not just on write) meant the live SIT sheet could be repaired with an ordinary respondent-facing GET request instead of a special admin/repair action — no new server-side surface was needed.
